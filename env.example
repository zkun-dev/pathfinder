# PathFinder 环境变量配置示例
# 
# ⚠️ 重要：所有环境都必须配置 VITE_API_BASE_URL
# 
# 使用方法：
# 1. 复制此文件为 .env.development（开发环境）或 .env.production（生产环境）
# 2. 取消注释并修改 VITE_API_BASE_URL 为你的后端地址
# 
# 注意：这些文件已被 .gitignore 忽略，不会提交到仓库

# API 基础地址（必需）
# 必须使用完整的 URL（包含协议 http:// 或 https://）
# 开发环境示例：http://localhost:3001/api
# 生产环境示例：https://your-backend-domain.com/api
VITE_API_BASE_URL=http://localhost:3001/api

# API 请求超时时间（毫秒，可选）
# 默认：开发环境 30秒，生产环境 10秒
# VITE_API_TIMEOUT=30000
